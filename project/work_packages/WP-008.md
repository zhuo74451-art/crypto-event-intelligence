# WP-008 — Validate clean-checkout reproducibility

## Contract

```yaml
work_package_id: WP-008
milestone_id: MS-005
sequence: 8
execution_mode: validation_only
checkpoint_mode: artifact_only
continuation_mode: gpt_gate
artifact_mode: runtime_store
workspace_mode: temporary_clone
max_attempts: 1
timeout_minutes: 90
```

## Single outcome

Prove the exact candidate ref builds, runs, and reproduces logical research outputs in a temporary clean checkout.

## Source IDs

- `OUT-004`
- `RISK-007`
- `ACC-001`

## Dependencies

- `WP-007 or GATE-INTEGRATION-AUDIT no-change route`

## Allowed paths

- `README.md`
- `PROJECT_MAINLINE.md`
- `market_radar/intelligence/**`
- `schemas/intelligence/**`
- `scripts/intelligence/**`
- `tests/intelligence/**`
- `data/intelligence/integration/**`
- `data/intelligence/research/**`
- `docs/execution/lane_e/**`
- `.humanthink/runtime/${RUN_ID}/artifacts/WP-008/**`

## Forbidden paths

- `.github/workflows/**`
- `deploy/**`
- `infra/**`
- `secrets/**`
- `.env`
- `.env.*`
- `project/**`

## Forbidden actions

- `git_push`
- `force_push`
- `merge_branch`
- `merge_to_main`
- `create_release`
- `production_write`
- `external_publish`
- `credential_read_or_change`
- `paid_api_call`
- `start_daemon`
- `schedule_recurring_job`
- `install_system_service`
- `enable_order_execution`
- `delete_unrelated_files`
- `change_product_identity`
- `expand_approved_scope`
- `reorder_task_graph`
- `decompose_future_packages`
- `modify_source`
- `commit`
- `install_unlocked_dependency`
- `network_fetch`
- `reuse_dirty_workspace`

## Required work

1. Check out the exact candidate ref in a new temporary workspace.
2. Use only preconfigured and approved dependencies.
3. Run the full intelligence suite and canonical one-shot integration.
4. Recompute research integrity and compare logical artifacts.
5. Capture commands, exit codes, durations, evidence types, and the exact ref.
6. Leave the checkout clean.

## Expected outputs

- `.humanthink/runtime/${RUN_ID}/artifacts/WP-008/clean_validation.json`
- `.humanthink/runtime/${RUN_ID}/artifacts/WP-008/reproducibility.json`
- `.humanthink/runtime/${RUN_ID}/artifacts/WP-008/evidence_manifest.json`

## Deterministic verification

### Run the complete intelligence test suite in the clean checkout.

```text
python -X utf8 -m pytest tests/intelligence/ -q
```

Expected exit code: `0`  
Timeout: `2400 seconds`

### Run the canonical one-shot path.

```text
python -X utf8 scripts/intelligence/integration/run_lane_e_pipeline.py --producer-lock docs/execution/lane_e/PRODUCER_LOCKS.yaml --one-shot
```

Expected exit code: `0`  
Timeout: `1200 seconds`

### Verify final research integrity.

```text
python -X utf8 scripts/intelligence/research/audit_research_integrity.py
```

Expected exit code: `0`  
Timeout: `600 seconds`

## Validation evidence

- Exact candidate SHA.
- Clean checkout and dependency state.
- All deterministic commands, exit codes, and durations.
- Logical output comparison to accepted candidate artifacts.
- No tracked modification.

## Cheapest falsification

- Strongest counterexample: The candidate only passes in the development worktree.
- Cheapest test: Run the canonical path from a fresh temporary checkout at the exact SHA.

## Stop conditions

- The candidate SHA cannot be checked out.
- Validation requires unapproved network or credentials.
- Logical outputs differ without an explained deterministic source.
- The clean checkout becomes modified.

## Handoff evidence

- clean_validation.json
- reproducibility.json
- evidence manifest
- exact SHA
- clean-worktree proof

## Result contract

The package must return exactly one normalized result: `accepted`, `accepted_no_change`, `hard_blocked`, `owner_required`, `replan_required`, `scope_conflict`, `budget_exhausted`, or `aborted_limit_reached`. Internal retries are finite and do not create task-graph edges.
