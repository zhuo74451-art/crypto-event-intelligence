# WP-010 — Run post-seal acceptance on the exact final ref

## Contract

```yaml
work_package_id: WP-010
milestone_id: MS-005
sequence: 10
execution_mode: validation_only
checkpoint_mode: artifact_only
continuation_mode: gpt_gate
artifact_mode: runtime_store
workspace_mode: temporary_clone
max_attempts: 1
timeout_minutes: 75
```

## Single outcome

Independently verify that the final seal, code, artifacts, hashes, and tests agree and then emit the final gate evidence.

## Source IDs

- `OUT-005`
- `RISK-007`
- `ACC-002`

## Dependencies

- `WP-009`

## Allowed paths

- `README.md`
- `PROJECT_MAINLINE.md`
- `docs/PROJECT_STATUS.md`
- `docs/execution/lane_e/**`
- `market_radar/intelligence/**`
- `schemas/intelligence/**`
- `scripts/intelligence/**`
- `tests/intelligence/**`
- `data/intelligence/integration/**`
- `data/intelligence/research/**`
- `.humanthink/runtime/${RUN_ID}/artifacts/WP-010/**`

## Forbidden paths

- `.github/workflows/**`
- `deploy/**`
- `infra/**`
- `secrets/**`
- `.env`
- `.env.*`
- `project/**`

## Forbidden actions

- `git_push`
- `force_push`
- `merge_branch`
- `merge_to_main`
- `create_release`
- `production_write`
- `external_publish`
- `credential_read_or_change`
- `paid_api_call`
- `start_daemon`
- `schedule_recurring_job`
- `install_system_service`
- `enable_order_execution`
- `delete_unrelated_files`
- `change_product_identity`
- `expand_approved_scope`
- `reorder_task_graph`
- `decompose_future_packages`
- `modify_source`
- `commit`
- `network_fetch`
- `reinterpret_scope`

## Required work

1. Check out the exact final seal ref in a new temporary workspace.
2. Re-run final tests and integrity checks.
3. Recompute all declared manifest hashes, counts, producer refs, and readiness flags.
4. Verify reports agree with Git and no tracked file changes.
5. Emit the final evidence bundle for GATE-FINAL-ACCEPTANCE.
6. Do not continue after a terminal decision.

## Expected outputs

- `.humanthink/runtime/${RUN_ID}/artifacts/WP-010/post_seal_acceptance.json`
- `.humanthink/runtime/${RUN_ID}/artifacts/WP-010/final_report.md`
- `.humanthink/runtime/${RUN_ID}/artifacts/WP-010/evidence_manifest.json`

## Deterministic verification

### Run final intelligence tests at the exact seal ref.

```text
python -X utf8 -m pytest tests/intelligence/ -q
```

Expected exit code: `0`  
Timeout: `2400 seconds`

### Run final integrity audit.

```text
python -X utf8 scripts/intelligence/research/audit_research_integrity.py
```

Expected exit code: `0`  
Timeout: `600 seconds`

### Prove post-seal validation made no tracked change.

```text
git diff --exit-code && git diff --cached --exit-code
```

Expected exit code: `0`  
Timeout: `60 seconds`

## Validation evidence

- Exact final ref.
- Final test and audit results.
- Manifest hashes and counts recomputed from actual files.
- Execution state and reports agree with Git.
- Fixed terminal notification payload.

## Cheapest falsification

- Strongest counterexample: The final seal is internally consistent but points at the wrong implementation or stale artifact.
- Cheapest test: Recompute all declared refs and hashes from the exact final checkout.

## Stop conditions

- Final ref differs from the seal input.
- Any declared hash, count, or readiness flag disagrees with files.
- Validation modifies tracked files.
- A terminal decision lacks complete evidence.

## Handoff evidence

- post_seal_acceptance.json
- final report
- evidence manifest
- clean-worktree proof
- terminal decision inputs

## Result contract

The package must return exactly one normalized result: `accepted`, `accepted_no_change`, `hard_blocked`, `owner_required`, `replan_required`, `scope_conflict`, `budget_exhausted`, or `aborted_limit_reached`. Internal retries are finite and do not create task-graph edges.
