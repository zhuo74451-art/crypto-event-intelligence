# WP-009 — Write the final Lane E integration seal

## Contract

```yaml
work_package_id: WP-009
milestone_id: MS-005
sequence: 9
execution_mode: modify
checkpoint_mode: commit
continuation_mode: automatic
artifact_mode: project_tracked
workspace_mode: current_worktree
max_attempts: 1
timeout_minutes: 45
```

## Single outcome

Record the accepted implementation ref, producer locks, artifact hashes, validation evidence, supported boundaries, and known limitations.

## Source IDs

- `OUT-005`
- `DEC-002`
- `CON-006`
- `RISK-007`

## Dependencies

- `GATE-MILESTONE-VALIDATION`

## Allowed paths

- `docs/execution/lane_e/EXECUTION_STATE.yaml`
- `docs/execution/lane_e/INTEGRATION_MANIFEST.yaml`
- `docs/execution/lane_e/FINAL_ACCEPTANCE.md`
- `docs/intelligence/integration/INTERNAL_VERSION_READINESS_V1.md`
- `docs/PROJECT_STATUS.md`

## Forbidden paths

- `.github/workflows/**`
- `deploy/**`
- `infra/**`
- `secrets/**`
- `.env`
- `.env.*`
- `market_radar/**`
- `scripts/**`
- `tests/**`
- `schemas/**`
- `data/**`

## Forbidden actions

- `git_push`
- `force_push`
- `merge_branch`
- `merge_to_main`
- `create_release`
- `production_write`
- `external_publish`
- `credential_read_or_change`
- `paid_api_call`
- `start_daemon`
- `schedule_recurring_job`
- `install_system_service`
- `enable_order_execution`
- `delete_unrelated_files`
- `change_product_identity`
- `expand_approved_scope`
- `reorder_task_graph`
- `decompose_future_packages`
- `change_code`
- `change_generated_artifact`
- `claim_unvalidated_capability`
- `record_self_referential_commit_sha`

## Required work

1. Write only the final state, manifest, acceptance report, and bounded project status update.
2. Record the accepted implementation ref rather than the seal commit itself.
3. Recompute and record all declared hashes and counts.
4. Preserve unsupported claim flags as false.
5. List known limitations and reopen conditions.
6. Create one local checkpoint commit.

## Expected outputs

- `Final Lane E execution state.`
- `Final integration manifest.`
- `Final acceptance report.`
- `Updated project status limited to the accepted milestone.`

## Deterministic verification

### Check final state, producer presence, and unsupported boundaries.

```text
python -X utf8 -c "import yaml; s=yaml.safe_load(open('docs/execution/lane_e/EXECUTION_STATE.yaml',encoding='utf-8')); m=yaml.safe_load(open('docs/execution/lane_e/INTEGRATION_MANIFEST.yaml',encoding='utf-8')); assert s['lane_status']=='accepted_real_producer_integration_pilot'; assert m['integrated_producers']; assert not any(m['unsupported_claims'].values())"
```

Expected exit code: `0`  
Timeout: `120 seconds`

### Check final tracked documentation for whitespace errors.

```text
git diff --check
```

Expected exit code: `0`  
Timeout: `60 seconds`

## Validation evidence

- Accepted implementation SHA from WP-008 input evidence.
- Producer lock and generated artifact hashes.
- Clean validation evidence refs.
- Explicit unsupported claim flags.
- No self-referential final SHA.

## Cheapest falsification

- Strongest counterexample: The seal claims a result that the accepted validation evidence does not support.
- Cheapest test: Compare every readiness flag and count to WP-008 evidence and Git objects.

## Stop conditions

- Any seal claim lacks accepted evidence.
- The implementation ref or artifact hash is ambiguous.
- A documentation change would alter code or product scope.

## Handoff evidence

- local seal commit
- manifest
- execution state
- acceptance report
- claim-to-evidence consistency check

## Result contract

The package must return exactly one normalized result: `accepted`, `accepted_no_change`, `hard_blocked`, `owner_required`, `replan_required`, `scope_conflict`, `budget_exhausted`, or `aborted_limit_reached`. Internal retries are finite and do not create task-graph edges.
