# WP-002 — Materialize only the approved Lane E candidate surface

## Contract

```yaml
work_package_id: WP-002
milestone_id: MS-002
sequence: 2
execution_mode: modify
checkpoint_mode: commit
continuation_mode: automatic
artifact_mode: project_tracked
workspace_mode: current_worktree
max_attempts: 2
timeout_minutes: 75
```

## Single outcome

Bring the exact RETAIN/ADAPT Lane E files into the current integration line with provenance and without importing quarantined history.

## Source IDs

- `OUT-001`
- `DEC-003`
- `DEC-005`
- `RISK-005`
- `NOGO-001`
- `NOGO-002`

## Dependencies

- `GATE-COMPAT`

## Allowed paths

- `project/runtime/effective_WP-002.yaml`
- `market_radar/intelligence/integration/**`
- `market_radar/intelligence/research/**`
- `schemas/intelligence/integration/**`
- `schemas/intelligence/research/**`
- `scripts/intelligence/integration/**`
- `scripts/intelligence/research/**`
- `tests/intelligence/integration/**`
- `tests/intelligence/research/**`
- `docs/intelligence/integration/**`
- `docs/intelligence/research/**`
- `docs/execution/lane_e/**`

## Forbidden paths

- `.github/workflows/**`
- `deploy/**`
- `infra/**`
- `secrets/**`
- `.env`
- `.env.*`
- `data/intelligence/historical_macro/**`
- `data/intelligence/historical_market/**`
- `data/intelligence/strategy_replay/**`
- `data/intelligence/validation/**`

## Forbidden actions

- `git_push`
- `force_push`
- `merge_branch`
- `merge_to_main`
- `create_release`
- `production_write`
- `external_publish`
- `credential_read_or_change`
- `paid_api_call`
- `start_daemon`
- `schedule_recurring_job`
- `install_system_service`
- `enable_order_execution`
- `delete_unrelated_files`
- `change_product_identity`
- `expand_approved_scope`
- `reorder_task_graph`
- `decompose_future_packages`
- `bulk_merge_old_lane_e`
- `cherry_pick_unreviewed_commit`
- `install_dependency`
- `copy_generated_sample_outputs`

## Required work

1. Read the gate-generated effective package and refuse any path outside both allowlists.
2. Materialize only selected RETAIN or ADAPT files from the exact old Lane E ref.
3. Preserve provenance for every copied or rewritten file.
4. Do not copy historical generated outputs as current product truth.
5. Adapt imports, schemas, and entrypoints only as needed for the current mainline.
6. Add focused tests for every adapted behavior.

## Expected outputs

- `Approved integration and research source files present on the planning-derived work branch.`
- `docs/execution/lane_e/CANDIDATE_PROVENANCE.yaml`
- `Targeted tests for every adapted entrypoint.`

## Deterministic verification

### Run the materialized Lane E candidate test surface.

```text
python -X utf8 -m pytest tests/intelligence/integration/ tests/intelligence/research/ -q
```

Expected exit code: `0`  
Timeout: `1200 seconds`

### Check exact provenance and quarantine compliance.

```text
python -X utf8 -c "import yaml; d=yaml.safe_load(open('docs/execution/lane_e/CANDIDATE_PROVENANCE.yaml',encoding='utf-8')); assert d['source_ref']=='8dfa3231826707df188be7152c34f00062b3606e'; assert not d['quarantined_files_imported']"
```

Expected exit code: `0`  
Timeout: `60 seconds`

## Validation evidence

- Gate-generated exact selected file list.
- Per-file disposition and adaptation note.
- No old generated output copied as product truth.
- No new dependency or service introduced.

## Cheapest falsification

- Strongest counterexample: A copied file silently restores stale producer assumptions or sample defaults.
- Cheapest test: Search the materialized surface for old branch SHAs, empty-producer defaults, hardcoded sample records, and absolute paths.

## Stop conditions

- The effective package is absent or exceeds the static allowlist.
- No safe subset of Lane E can be materialized.
- Materialization requires product scope expansion or a new infrastructure platform.
- Tests require production, credentials, paid access, or recurring services.

## Handoff evidence

- Local checkpoint commit
- candidate provenance file
- selected and quarantined file inventory
- targeted test results
- changed-file allowlist proof

## Result contract

The package must return exactly one normalized result: `accepted`, `accepted_no_change`, `hard_blocked`, `owner_required`, `replan_required`, `scope_conflict`, `budget_exhausted`, or `aborted_limit_reached`. Internal retries are finite and do not create task-graph edges.
