# WP-003 — Build canonical A-D producer locks

## Contract

```yaml
work_package_id: WP-003
milestone_id: MS-002
sequence: 3
execution_mode: modify
checkpoint_mode: commit
continuation_mode: automatic
artifact_mode: project_tracked
workspace_mode: current_worktree
max_attempts: 2
timeout_minutes: 75
```

## Single outcome

Create fail-closed producer locks for exact A-D refs, artifacts, counts, hashes, and supported claim boundaries.

## Source IDs

- `OUT-002`
- `DEC-002`
- `DEC-006`
- `RISK-001`
- `RISK-004`

## Dependencies

- `WP-002 or GATE-COMPAT skip route`

## Allowed paths

- `docs/execution/lane_e/**`
- `data/intelligence/integration/upstream/**`
- `scripts/intelligence/integration/lock_producers.py`
- `scripts/intelligence/integration/check_producer_compatibility.py`
- `tests/intelligence/integration/**`

## Forbidden paths

- `.github/workflows/**`
- `deploy/**`
- `infra/**`
- `secrets/**`
- `.env`
- `.env.*`
- `data/intelligence/historical_macro/**`
- `data/intelligence/historical_market/**`
- `data/intelligence/strategy_replay/**`
- `data/intelligence/validation/**`

## Forbidden actions

- `git_push`
- `force_push`
- `merge_branch`
- `merge_to_main`
- `create_release`
- `production_write`
- `external_publish`
- `credential_read_or_change`
- `paid_api_call`
- `start_daemon`
- `schedule_recurring_job`
- `install_system_service`
- `enable_order_execution`
- `delete_unrelated_files`
- `change_product_identity`
- `expand_approved_scope`
- `reorder_task_graph`
- `decompose_future_packages`
- `mutate_upstream_artifact`
- `use_working_tree_hash_as_git_object_hash`
- `accept_placeholder_hash`
- `accept_stale_sha`
- `network_fetch`

## Required work

1. Lock exact producer refs and the accepted implementation basis for each artifact.
2. Recompute artifact hashes from actual bytes and verify declared record counts.
3. Copy or reference producer artifacts without mutating upstream sources.
4. Carry forward supported and unsupported claim boundaries.
5. Reject placeholders, stale SHAs, missing files, wrong types, and count mismatches.
6. Add fault injection for at least one corrupted producer artifact.

## Expected outputs

- `docs/execution/lane_e/PRODUCER_LOCKS.yaml`
- `docs/execution/lane_e/PRODUCER_LOCK_AUDIT.json`
- `data/intelligence/integration/upstream/ producer copies or immutable references`
- `producer-lock contract tests`

## Deterministic verification

### Verify exact producer refs, hashes, record counts, and boundaries.

```text
python -X utf8 scripts/intelligence/integration/lock_producers.py --verify-only
```

Expected exit code: `0`  
Timeout: `600 seconds`

### Run producer-lock and integration contract tests.

```text
python -X utf8 -m pytest tests/intelligence/integration/ -q
```

Expected exit code: `0`  
Timeout: `1200 seconds`

## Validation evidence

- Lane A ref 9aaabc82f34141e6797d3f92b773d5c463ad99b8.
- Lane B ref ec8952b600ffceebeabcb50ac11e3b182e76c5e2.
- Lane C ref 72984d8b0fe17dc188239c3c050e082c180853b4.
- Lane D ref a6b5baf1e0d5060b5375ce12c68e04ee547efb88.
- Git-object or declared artifact byte hashes and record counts.
- Unsupported claim boundaries copied into the consumer lock.

## Cheapest falsification

- Strongest counterexample: A producer copy passes because the lock trusts a stored equality flag rather than actual bytes.
- Cheapest test: Corrupt one copied artifact byte and require a stable producer-hash failure code.

## Stop conditions

- Any pinned ref does not resolve.
- Any required artifact, hash, record count, or schema cannot be verified.
- Lane B state and manifest conflict cannot be safely resolved within approved semantics.
- A producer boundary would need to be weakened.

## Handoff evidence

- producer locks
- producer audit
- fault-injection results
- local checkpoint commit
- exact ref and hash matrix

## Result contract

The package must return exactly one normalized result: `accepted`, `accepted_no_change`, `hard_blocked`, `owner_required`, `replan_required`, `scope_conflict`, `budget_exhausted`, or `aborted_limit_reached`. Internal retries are finite and do not create task-graph edges.
