# WP-006 — Run independent integration and reuse audit

## Contract

```yaml
work_package_id: WP-006
milestone_id: MS-004
sequence: 6
execution_mode: audit_readonly
checkpoint_mode: artifact_only
continuation_mode: gpt_gate
artifact_mode: runtime_store
workspace_mode: clean_worktree
max_attempts: 1
timeout_minutes: 90
```

## Single outcome

Produce an independent fail-closed audit of the candidate integration while leaving tracked files unchanged.

## Source IDs

- `OUT-004`
- `RISK-001`
- `RISK-002`
- `RISK-003`
- `RISK-007`

## Dependencies

- `WP-005`

## Allowed paths

- `market_radar/intelligence/**`
- `schemas/intelligence/**`
- `scripts/intelligence/**`
- `tests/intelligence/**`
- `data/intelligence/integration/**`
- `data/intelligence/research/**`
- `docs/execution/lane_e/**`
- `.humanthink/runtime/${RUN_ID}/artifacts/WP-006/**`

## Forbidden paths

- `.github/workflows/**`
- `deploy/**`
- `infra/**`
- `secrets/**`
- `.env`
- `.env.*`
- `project/**`

## Forbidden actions

- `git_push`
- `force_push`
- `merge_branch`
- `merge_to_main`
- `create_release`
- `production_write`
- `external_publish`
- `credential_read_or_change`
- `paid_api_call`
- `start_daemon`
- `schedule_recurring_job`
- `install_system_service`
- `enable_order_execution`
- `delete_unrelated_files`
- `change_product_identity`
- `expand_approved_scope`
- `reorder_task_graph`
- `decompose_future_packages`
- `modify_tracked_file`
- `commit`
- `install_dependency`
- `network_fetch`

## Required work

1. Start from a clean candidate ref and keep tracked files unchanged.
2. Recompute producer and output hashes and record counts.
3. Verify all graph references and claim evidence links.
4. Run two clean logical rebuilds.
5. Inject stale ref, corrupt hash, missing producer, sample fallback, unsupported claim, and absolute-path faults.
6. Separate environment absence from implementation failure.
7. Emit blocking and nonblocking findings with stable IDs.

## Expected outputs

- `.humanthink/runtime/${RUN_ID}/artifacts/WP-006/integration_audit.json`
- `.humanthink/runtime/${RUN_ID}/artifacts/WP-006/falsification_report.md`
- `.humanthink/runtime/${RUN_ID}/artifacts/WP-006/rebuild_comparison.json`

## Deterministic verification

### Run independent integrated tests.

```text
python -X utf8 -m pytest tests/intelligence/integration/ tests/intelligence/research/ -q
```

Expected exit code: `0`  
Timeout: `1500 seconds`

### Prove the audit did not modify tracked files.

```text
git diff --exit-code && git diff --cached --exit-code
```

Expected exit code: `0`  
Timeout: `60 seconds`

## Validation evidence

- Producer ref/hash/count audit.
- Lineage and reference-integrity audit.
- Two-empty-directory logical rebuild comparison.
- Stale-ref, missing-producer, corrupted-hash, sample-fallback, unsupported-claim, and absolute-path fault injection.
- Report-versus-Git consistency.

## Cheapest falsification

- Strongest counterexample: The candidate passes its own tests but fails from a clean worktree or under stale-ref injection.
- Cheapest test: Run the canonical path in a clean worktree with one intentionally stale producer lock.

## Stop conditions

- The candidate ref is not exact or the worktree is dirty before audit.
- Audit tooling modifies tracked files.
- A blocking finding lacks reproducible evidence.
- The audit cannot separate implementation failure from environment absence.

## Handoff evidence

- integration_audit.json
- falsification report
- rebuild comparison
- tracked-file integrity proof
- exact candidate SHA

## Result contract

The package must return exactly one normalized result: `accepted`, `accepted_no_change`, `hard_blocked`, `owner_required`, `replan_required`, `scope_conflict`, `budget_exhausted`, or `aborted_limit_reached`. Internal retries are finite and do not create task-graph edges.
