# Market Radar v1.12-M — Deterministic Gate Time Hardening Handoff

**Run**: 2026-06-05
**Status**: PASSED
**Version**: v1.12-M
**Run ID**: 20260604_202718
**Task ID**: 20260604_202718.r22

---

## What was done

1. Diagnosed the pre-existing time-dependent failure in `test_result_blocked_cooldown_ge_1`
2. Root cause: v112i runner used `_now_cn()` (current system time), prior state fixtures have cooldowns anchored to 2026-06-04, so after June 4 all cooldowns expired
3. Fixed the runner to use a deterministic `DETERMINISTIC_EVALUATED_AT = datetime(2026, 6, 4, 22, 30, 0, tzinfo=CN_TZ)`
4. Added `deterministic_clock=true`, `evaluated_at`, and `time_dependent_test_risk=false` to the result JSON
5. Verified the gate module already supports explicit `evaluated_at` in all functions — no module changes needed
6. Verified v112k canonical replay continues to pass (first_pass_eligible_reblocked_count=9, idempotency=true)
7. Verified v112l state key hardening continues to pass (no gate evaluation, no time dependency)
8. Ran full test suite: 1056/1056 tests pass (v112a through v112l)

## Key changes

| File | Change |
|------|--------|
| `scripts/run_market_radar_v112i_dedupe_cooldown_gate.py` | `_now_cn()` → `DETERMINISTIC_EVALUATED_AT` constant |
| `results/market_radar_v112m_deterministic_gate_time_hardening_result.json` | New — v112m result |
| `runs/market_radar/v112m_deterministic_gate_time_hardening.md` | New — report |
| `runs/market_radar/v112m_deterministic_gate_time_hardening_handoff.md` | New — this file |

## What was NOT changed

- Gate module core logic — already supported `evaluated_at`
- Any test file — all already used fixed `evaluated_at` in unit tests
- v112k / v112l runners — already deterministic
- No assertions lowered, no tests deleted, no tests skipped
- No external API/AI calls, no TG send, no daemon, no live data

## Acceptance criteria check

| Criteria | Status |
|----------|--------|
| test_market_radar_dedupe_cooldown_gate_v112i.py: 49/49 | ✅ |
| cooldown supports explicit evaluated_at | ✅ |
| v112i runner uses deterministic clock | ✅ |
| result JSON has deterministic_clock=true | ✅ |
| test_result_blocked_cooldown_ge_1 no longer time-drift fails | ✅ |
| active cooldown block test preserved | ✅ |
| expired cooldown pass test preserved | ✅ |
| No assertions lowered/deleted/skipped | ✅ |
| v112k canonical replay continues to pass | ✅ |
| v112l canonical state key hardening continues to pass | ✅ |
| v112a-v112j legacy tests all pass | ✅ |
| debug_leak_count=0 | ✅ |
| secret_leak_count=0 | ✅ |
| real_tg_sent=false | ✅ |
| external_api_called=false | ✅ |
| external_ai_called=false | ✅ |
| daemon_started=false | ✅ |
| live_ready=false | ✅ |

## Conclusion

The time-dependent failure was fixed by making the v112i runner use a fixed `evaluated_at` timestamp. The gate module already supported this parameter; only the runner needed updating. All 1056 tests across v112a through v112l pass. The cooldown gate is now deterministic, reproducible, and replayable regardless of system clock.

---

*Generated by v1.12-M at 2026-06-05*
